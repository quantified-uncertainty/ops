apiVersion: v1
kind: Service
metadata:
  name: {{ include "name" . }}
  labels:
    app: {{ include "name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.port }}
  selector:
    app: {{ include "name" . }}
